# 📱 CleanBack Mobile App — Техническая спецификация (v1.0)

> **Проект:** Мобильное приложение для службы химчистки обуви CleanBack  
> **Платформы:** iOS, Android (Flutter)  
> **Язык:** Русский (RU)  
> **Статус:** MVP (Mock Data) → Production (Supabase)  
> **Дата:** 2026-02-18

---

## 1. 🎯 Обзор проекта

### 1.1 Цель
Разработать кроссплатформенное приложение для заказа химчистки обуви с курьерской доставкой. 
**Ключевая особенность MVP:** Возможность оформления заказа **без регистрации** (гостевой режим).

### 1.2 Основные функции
1.  **Заказ услуги:** Выбор типа чистки, количества пар, ввод адреса и контакта.
2.  **Отслеживание:** Просмотр статусов заказа (Принят, В работе, Готов, Доставлен).
3.  **Информирование:** Новости, акции, пуш-уведомления.
4.  **Оплата:** Архитектура под онлайн-оплату (в MVP — оплата курьеру/после).

### 1.3 Логистика
-   **Пункты приема:** Отсутствуют.
-   **Доставка:** Только курьерская (забор и возврат обуви).

---

## 2. 🎨 Дизайн-система (UI Kit)

Стиль должен соответствовать действующему сайту `cleanback.ru` (минимализм, премиальность, контраст).

### 2.1 Цветовая палитра
| Название | Hex Code | Использование |
|----------|----------|---------------|
| `primary` | `#000000` | Текст заголовков, основные иконки |
| `background` | `#FFFFFF` | Фон экранов |
| `secondary_bg` | `#F5F5F7` | Фон карточек, блоков |
| `accent` | `#E63946` | Кнопки CTA, акценты, статусы (как на сайте) |
| `text_secondary` | `#8E8E93` | Второстепенный текст, подсказки |
| `success` | `#34C759` | Статус "Готов", "Доставлен" |
| `warning` | `#FF9500` | Статус "В работе" |

### 2.2 Типографика
-   **Шрифт:** `Montserrat` (заголовки), `Roboto` (основной текст).
-   **Размеры:**
    -   `H1`: 24px, Bold
    -   `H2`: 20px, SemiBold
    -   `Body`: 16px, Regular
    -   `Caption`: 14px, Regular

### 2.3 Компоненты
-   **Кнопки:** Прямоугольные, скругление 8-12px. Основная — залитая (`accent`), Вторичная — контурная.
-   **Поля ввода:** Подчеркивание или легкий фон, высота 48px.
-   **Карточки:** Тень `boxShadow: [0, 2, 10, 0.05]`, скругление 12px.

---

## 3. ⚙️ Техническая архитектура

### 3.1 Стек технологий
-   **Framework:** Flutter 3.x (Dart 3.x)
-   **State Management:** `flutter_riverpod` (рекомендуется для AI-генерации из-за читаемости)
-   **Navigation:** `go_router` (декларативная навигация)
-   **Local DB:** `hive` или `isar` (для хранения заказов в MVP)
-   **Network:** `dio` (для будущей интеграции Supabase)
-   **Backend (Future):** Supabase (Auth, Database, Edge Functions)
-   **Push Notifications:** `firebase_messaging` + `flutter_local_notifications`
-   **Payments:** Абстракция `PaymentService` (в MVP — заглушка)

### 3.2 Структура проекта (Clean Architecture)
```text
lib/
├── core/
│   ├── constants/       # Цвета, строки, эндпоинты
│   ├── theme/           # ThemeData, TextStyles
│   ├── utils/           # Валидаторы, форматеры
│   └── network/         # Dio client, Interceptors
├── data/
│   ├── models/          # DTO (Json Serializable)
│   ├── repositories/    # Реализации репозиториев (Mock vs Supabase)
│   └── sources/         # LocalDataSource (Hive), RemoteDataSource
├── domain/
│   ├── entities/        # Чистые объекты бизнеса
│   └── repositories/    # Абстракции репозиториев
├── presentation/
│   ├── screens/         # Экраны (Home, Order, Profile...)
│   ├── widgets/         # Переиспользуемые виджеты
│   └── providers/       # Riverpod providers
└── main.dart