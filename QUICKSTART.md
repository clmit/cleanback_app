# CleanBack App - Быстрый старт

## ✅ Установка завершена

Приложение готово к запуску!

## 🚀 Запуск приложения

### Вариант 1: Через PowerShell (рекомендуется)

```powershell
cd C:\Users\Митя\Documents\Projects\clb-app
flutter run -d chrome
```

### Вариант 2: Через скрипт

```powershell
.\run-app.ps1
```

Выберите опцию 1 для запуска в Chrome.

## 📱 Доступные команды

| Команда | Описание |
|---------|----------|
| `flutter run -d chrome` | Запуск в браузере Chrome |
| `flutter run -d windows` | Запуск как Windows-приложение |
| `flutter run` | Запуск на подключенном Android-устройстве |
| `flutter doctor -v` | Диагностика Flutter |
| `flutter clean` | Очистка проекта |
| `flutter pub get` | Установка зависимостей |

## 🎯 Функции MVP

1. **Главный экран** - кнопки заказа и отслеживания
2. **Оформление заказа** - выбор типа чистки, адреса, телефона
3. **Отслеживание** - просмотр статусов заказов
4. **Новости** - акции и новости компании

## 🔧 Если что-то не работает

### Ошибка "Flutter not found"
```powershell
$env:Path += ";C:\src\flutter\bin"
```

### Ошибка компиляции
```powershell
flutter clean
flutter pub get
flutter run -d chrome
```

### Проблемы с Chrome
Убедитесь, что Chrome установлен и закрыт перед запуском.

## 📂 Структура проекта

```
lib/
├── core/           # Константы, тема, роутинг
├── domain/         # Сущности и репозитории
├── data/           # Модели и Mock-данные
└── presentation/   # Экраны, виджеты, провайдеры
```

## 🎨 Дизайн-система

- **Цвета:** Черный (#000000), Белый (#FFFFFF), Акцент (#E63946)
- **Шрифты:** Montserrat (заголовки), Roboto (текст)

---

**Приложение запущено!** Откройте браузер и наслаждайтесь работой CleanBack.
